#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:misp) :silent t)
  )

(defpackage :ros.script.misp.3819006641
  (:use :cl))
(in-package :ros.script.misp.3819006641)

(defun main (&rest argv)
  (declare (ignorable argv))
  (misp:misp))
;;; vim: set ft=lisp lisp:
